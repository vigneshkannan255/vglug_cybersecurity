sudo 
apt - Package management
    sudo apt install package_name 
    sudo apt remove package_name
    sudo apt list
    sudo apt list --installed| grep bash
    sudo apt update
    sudo apt upgrade


echo "hello"
echo $PATH
echo $(date)

usermod -s /bin/ksh username


systemctl - service mangement

df -Th - Filesystem 
lsblk - disk info
pvs - Physical volume information
vgs - Volume group information
lvs - Logical volume information

reboot 
ssh username@hostname
